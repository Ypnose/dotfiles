# ---------------------------------------------------------------------+
# file:     .aliases                                                   +
# author:   Ypnose - http://ywstd.fr                                   +
# ---------------------------------------------------------------------+

# -- ALIASES -----------------------------------------------------------
alias j='jupp'
alias ls='ls -Fh'
alias sx='startx'
alias up='sudo apt-get update && sudo apt-get upgrade'
alias grep='grep -I'
