#!/bin/sh
# Script by Ypnose - http://ywstd.fr
# Just a simple snippet.

# No '-S' with curl(1) to avoid exit code 23
curl -s "http://airparif.fr/" | awk '
	/<a data-tooltip="def-head"/ {
		gsub(/^.*<strong>|<\/strong>.*$/,"")
		print $0
		exit
	}
'

exit
