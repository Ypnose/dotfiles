# ---------------------------------------------------------------------+
# file:     .aliases                                                   +
# author:   Ypnose - http://ywstd.fr                                   +
# ---------------------------------------------------------------------+

# -- ALIASES -----------------------------------------------------------
alias ls='ls -Fh'
alias grep='grep -I'
alias nocom='grep -E -v -n -e "^[[:blank:]]*#" -e "^$"'
